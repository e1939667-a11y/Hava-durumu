<Window x:Class="HavaDurumu.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hava Durumu"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Height="600"
        Width="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid x:Name="MainGrid" Background="#1F1F1F" Opacity="0.95">
        <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0">
            <!-- Başlık -->
            <TextBlock Text="Hava Durumu" FontSize="24" FontWeight="Bold" Foreground="White" Margin="10" />
            
            <!-- Anlık Hava (Büyük Görünüm) -->
            <Border BorderBrush="#333" BorderThickness="1" Margin="10,0,10,10">
                <StackPanel Padding="15">
                    <TextBlock Text="Bugün" FontSize="18" Foreground="White" FontWeight="Bold" />
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock x:Name="CurrentTemp" Text="25°C" FontSize="48" Foreground="#FFD700" FontWeight="Bold" />
                        <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                            <TextBlock x:Name="CurrentCondition" Text="Açık" FontSize="16" Foreground="White" />
                            <TextBlock x:Name="CurrentDesc" Text="Nemlilik: 65%" FontSize="12" Foreground="#AAA" />
                            <TextBlock x:Name="CurrentWind" Text="Rüzgar: 10 km/s" FontSize="12" Foreground="#AAA" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <!-- 5 Günlük Tahmin -->
            <Border x:Name="FiveDayBorder" BorderBrush="#333" BorderThickness="1" Margin="10,0,10,10">
                <StackPanel Padding="15">
                    <TextBlock Text="5 Günlük Tahmin" FontSize="14" Foreground="White" FontWeight="Bold" Margin="0,0,0,10" />
                    <ItemsControl x:Name="FiveDaysList" Height="80" />
                </StackPanel>
            </Border>
            
            <!-- Saatlik Tahmin (Küçük Bar) -->
            <Border x:Name="HourlyBorder" BorderBrush="#333" BorderThickness="1" Margin="10,0,10,10">
                <StackPanel Padding="15">
                    <TextBlock Text="Saatlik Tahmin" FontSize="12" Foreground="#AAA" Margin="0,0,0,5" />
                    <ItemsControl x:Name="HourlyList" Height="60" />
                </StackPanel>
            </Border>
            
            <!-- Ay Evresini Göster -->
            <TextBlock x:Name="MoonPhase" Text="🌙 Ay Evresini Yükle" FontSize="12" Foreground="#FFD700" Margin="10" />
        </StackPanel>
    </Grid>
</Window>
